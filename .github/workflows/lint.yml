---
name: Lint

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: sudo apt install moreutils black parallel shellcheck pylint
    - name: Lint scripts
      run: make lint
