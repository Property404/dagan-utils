---
name: Lint and Test

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Install dependencies
      run: sudo apt install black shellcheck pylint jq
    - name: Lint scripts
      run: make lint
    - name: Run tests
      run: make test
